apiVersion: v1
kind: Service
metadata:
  name: {{ include "churn-radar.fullname" . }}-api
  labels:
    {{- include "churn-radar.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "churn-radar.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
